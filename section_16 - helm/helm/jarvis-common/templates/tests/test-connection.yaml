apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jarvis-common.fullname" . }}-test-connection"
  labels:
    {{- include "jarvis-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jarvis-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
