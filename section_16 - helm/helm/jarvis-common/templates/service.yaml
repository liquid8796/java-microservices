apiVersion: v1
kind: Service
metadata:
  name: {{ include "jarvis-common.fullname" . }}
  labels:
    {{- include "jarvis-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jarvis-common.selectorLabels" . | nindent 4 }}
